# Test runner configuration
[test]
# Match test files in any __tests__ directory or files ending in .test.ts
preload = []
# Timeout per test in milliseconds
timeout = 10000

# Coverage configuration (run with: bun test --coverage)
[test.coverage]
include = ["src/**/*.ts"]
exclude = ["src/**/*.test.ts", "src/__tests__/**"]
